build-dev:
  - makepot
  - cssmin
  - uglify
  - autoprefixer
  - shell
  - compress

build:
  - makepot
  - autoprefixer
  - cssmin
  - uglify
  - bumpup:minor
  - replace # Update version numbers, build faq.txt
  - concat # build readme.txt

release:
  - shell
  - compress

localize:
  - makepot
